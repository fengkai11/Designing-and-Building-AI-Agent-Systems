# 附录 E：Agent 源码阅读模板

阅读 Agent 项目源码时，不要从文件细节开始，而要先建立运行链路。本模板适用于 OpenClaw、AutoGPT、代码 Agent、浏览器 Agent、企业流程 Agent 等项目。

## 一、项目基本信息

```markdown
- 项目名称：
- 项目地址：
- 项目定位：
- 主要用户：
- 核心场景：
- 技术栈：
- 是否有前端：
- 是否有后端服务：
- 是否有本地 Runtime：
- 是否有插件/工具系统：
```

## 二、第一遍：看产品入口

要回答：用户如何开始一个任务？

检查位置：

```text
README
examples/
apps/
cli/
web/
server/
main.py
index.ts
```

记录：

```markdown
- 用户输入在哪里进入系统？
- 输入是自然语言、表单、命令行参数，还是 API？
- 系统创建了什么任务对象？
- 任务如何进入执行队列？
```

## 三、第二遍：画执行链路

尝试画出：

```text
User Input
  ↓
Task Creation
  ↓
Context Build
  ↓
Model Call
  ↓
Action Parse
  ↓
Tool Call
  ↓
State Update
  ↓
Stop / Continue / Approval
```

如果无法画出这条链路，说明还没有真正读懂项目。

## 四、第三遍：找核心抽象

重点寻找这些对象：

| 抽象 | 关注问题 |
|---|---|
| Agent | 它代表角色、任务，还是执行器？ |
| Task | 任务目标和状态如何表示？ |
| Tool | 工具如何注册和调用？ |
| Message | 模型上下文如何表示？ |
| Memory | 长期信息如何保存？ |
| State | 执行进度如何保存？ |
| Event | 日志和事件如何记录？ |
| Approval | 人工确认如何触发？ |

## 五、第四遍：看工具系统

记录：

```markdown
- 工具在哪里定义？
- 工具如何注册？
- 工具描述是否给模型看？
- 参数如何校验？
- 工具失败如何处理？
- 是否有权限分级？
- 是否有 dry-run？
- 是否有审计日志？
```

## 六、第五遍：看上下文构造

记录：

```markdown
- system prompt 在哪里？
- developer prompt 或运行规则在哪里？
- 历史消息如何裁剪？
- 工具结果如何进入上下文？
- 检索结果如何进入上下文？
- 是否有压缩/摘要机制？
- 是否有 token budget 控制？
```

## 七、第六遍：看状态与长任务

记录：

```markdown
- 任务状态有哪些？
- 是否支持暂停、恢复、取消？
- 是否支持 checkpoint？
- 失败是否可重试？
- 中间产物保存在哪里？
- 多任务是否并发？
```

## 八、第七遍：看安全与审批

记录：

```markdown
- 哪些动作会直接执行？
- 哪些动作需要用户确认？
- 是否限制 shell、文件、网络、邮件等工具？
- 是否防止 prompt injection？
- 是否有敏感信息保护？
- 是否有权限模型？
```

## 九、第八遍：看可观测性

记录：

```markdown
- 是否记录模型输入输出？
- 是否记录工具调用？
- 是否记录 token 和成本？
- 是否支持 trace 回放？
- 是否有 eval？
- 如何定位失败任务？
```

## 十、源码阅读输出模板

```markdown
# 项目源码阅读报告

## 1. 项目定位

## 2. 用户入口

## 3. 任务执行链路

```text
画出链路
```

## 4. 核心模块

| 模块 | 职责 | 关键文件 | 可借鉴点 |
|---|---|---|---|

## 5. 工具系统分析

## 6. 上下文系统分析

## 7. 状态与长任务分析

## 8. 安全与审批分析

## 9. 可观测与评估分析

## 10. 可复用设计

## 11. 不建议照搬的部分

## 12. 对自己项目的启发
```

## 十一、阅读源码时的警惕点

1. 不要把项目目录当成架构本身。
2. 不要因为某个项目没有某模块，就认为该模块不重要。
3. 不要照搬历史包袱。
4. 不要忽略产品场景差异。
5. 不要把闭源产品的外部行为写成确定内部实现。
6. 不要只读 happy path，要读失败、重试、权限和日志。
